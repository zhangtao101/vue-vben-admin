# 技能说明文档（Skills Guide）

> 本文档介绍本项目中可用的 **技能（Skills）**，分为「当前项目技能」与「全局技能」两部分，并说明它们的主要作用与适用场景。

---

## 一、什么是技能

技能（Skill）是面向特定领域或数据格式的**扩展能力包**，它内置了专家知识、标准化工作流（SOP）以及可执行的工具/脚本。当任务涉及某个领域时，加载对应技能可以显著提升结果的**正确性、执行效率与质量**。

技能按来源分为两类：

- **当前项目技能**：随本项目仓库存放于 `.codebuddy/skills/` 目录，针对本项目的业务平台（IPC 作业平台、MES 系统）与工程规范定制。
- **全局技能**：由运行环境（管理器 / 插件 / 用户配置）提供，不随本项目仓库分发，可在任意项目中通用（如文档处理、浏览器自动化、Vue 专项规范等）。

---

## 二、当前项目技能（`.codebuddy/skills/`）

这些技能与本项目的业务平台（IPC / MES）及工程实践强相关。

### 2.1 业务平台开发规范（IPC 作业平台）

基于 **Vue 3 + Ant Design Vue + VXE Grid** 的开发规范，用于快速搭建符合平台标准的页面与组件。

| 技能 | 主要作用 |
| --- | --- |
| `ipc-list-page` | 列表页开发规范：查询表单、表格展示、操作按钮等查询列表页面的创建 |
| `ipc-component-dev` | 业务组件开发规范：可复用组件结构、Props 定义、事件处理、工序步骤组件 |
| `ipc-drawer-form` | 抽屉表单开发规范：详情抽屉、新增/编辑抽屉，含表单布局、底部按钮、表单验证 |
| `ipc-api-integration` | API 服务开发规范：创建 API 服务文件，含接口定义、参数处理、错误处理、WebSocket 路径获取 |
| `ipc-page-structure` | 页面路由与菜单配置规范：路由定义、后端菜单转换、权限控制、路由守卫 |
| `ipc-internationalization` | 国际化开发规范：中英文语言包文件创建与管理，含 JSON 结构、`$t()` 使用、模块划分 |
| `ipc-websocket` | WebSocket 实时通信规范：封装、自动重连、心跳检测及在业务组件中的使用 |

### 2.2 业务平台开发规范（MES 系统）

基于 **Vue 3 + Vben Admin + ant-design-vue + VXE Grid** 的开发规范。

| 技能 | 主要作用 |
| --- | --- |
| `mes-list-page` | 列表页开发规范：查询列表页面的创建 |
| `mes-component-dev` | 业务组件开发规范：组件结构、Props 定义、事件处理、插槽使用 |
| `mes-drawer-form` | 抽屉表单开发规范：详情/新增/编辑抽屉，含表单布局、底部按钮插槽、表单验证 |
| `mes-api-integration` | API 服务开发规范：创建 API 服务文件，含接口定义、参数处理、错误处理 |
| `mes-page-structure` | 页面路由配置规范：路由、菜单、权限配置 |
| `mes-internationalization` | 国际化开发规范：中英文翻译文件创建与管理 |
| `mes-export-feature` | 导出功能开发规范：列表数据 Excel 导出（前端导出与后端接口导出两种方式） |

### 2.3 代码质量与评审

| 技能 | 主要作用 |
| --- | --- |
| `receiving-code-review` | 收到代码评审反馈时使用：在落实建议前先评估其合理性与技术正确性，避免盲目修改 |
| `requesting-code-review` | 完成任务、实现重要功能或合并前使用：主动发起评审以验证工作是否满足需求 |
| `verification-before-completion` | 准备声明"已完成/已修复/已通过"时使用：在提交或创建 PR 前运行验证并确认产出 |
| `systematic-debugging` | 遇到任何 Bug、测试失败或异常行为时使用：先定位根因再提出修复方案 |

### 2.4 Git 与提交规范

| 技能 | 主要作用 |
| --- | --- |
| `git-commit` | 自动生成符合项目规范的 Git 提交信息（生成提交信息、查看提交规范） |
| `commit-pre-check` | 提交前检查：对 git 所有改动做只读检查并输出问题清单（不修改代码） |

### 2.5 规划、设计与写作

| 技能 | 主要作用 |
| --- | --- |
| `brainstorming` | 任何创造性工作之前使用：在动手实现前先澄清用户意图、需求与设计 |
| `writing-plans` | 有多步任务的需求/规格时使用：先产出实现计划，再开始写代码 |
| `executing-plans` | 已有书面实现计划时使用：在带评审检查点的独立会话中按步骤执行 |
| `code-comments`（代码注释） | 为文件与函数生成规范注释：补全 L3 文件头与 JSDoc（参数、返回值、异常、`@since`） |
| `writing-skills` | 创建、编辑技能或部署前验证技能时使用 |
| `ui-ux-pro-max` | UI/UX 设计智能：提供可检索的设计知识库，辅助界面与体验优化 |
| `graphify` | 针对代码库、架构、文件关系或项目内容的问答：将输入转化为知识图谱，支持查询/路径/解释 |

---

## 三、全局技能（运行环境提供，非本项目仓库分发）

这些技能由运行环境托管，可在任意项目中通用，按用途归类如下。

### 3.1 文档与办公处理

| 技能 | 主要作用 |
| --- | --- |
| `pptx` | 任意涉及 `.pptx` 文件的场景：创建、读取、编辑、合并/拆分演示文稿 |
| `pdf` | 任意 PDF 操作：读取/抽取文本表格、合并拆分、旋转、加水印、表单填写、OCR 等 |
| `docx` | 创建、读取、编辑 Word 文档（`.docx`）：目录、标题、页码、信头、修订等 |
| `xlsx` | 电子表格文件（`.xlsx`/`.csv`/`.tsv`）读写与处理：列计算、格式化、清洗、转换 |

### 3.2 浏览器与自动化测试

| 技能 | 主要作用 |
| --- | --- |
| `agent-browser` | 浏览器自动化：打开网页、截图、抽取内容、点击元素、填写表单、测试 Web 流程 |
| `playwright-cli` | 基于 Playwright 的浏览器交互：Web 测试、表单填写、截图、数据抽取 |

### 3.3 Vue 专项规范（与本 Vue 项目高度相关）

| 技能 | 主要作用 |
| --- | --- |
| `vue-best-practices` | Vue.js 任务必用：推荐 `<script setup>` + Composition API + TypeScript（Vue 3 / SSR / Volar / vue-tsc） |
| `vue-testing-best-practices` | Vue.js 测试：Vitest、Vue Test Utils、组件测试、Mock、测试模式与 Playwright E2E |

### 3.4 多模态内容生成

| 技能 | 主要作用 |
| --- | --- |
| `多模态内容生成` | 文生视频、图生视频、文生图片、文生 3D 模型、图生 3D 模型，以及图片/视频特效 |

### 3.5 技能管理

| 技能            | 主要作用                                         |
| --------------- | ------------------------------------------------ |
| `skill-creator` | 创建有效技能或更新现有技能的指南                 |
| `find-skills`   | 当用户询问"如何做 X"时，帮助发现并安装适用的技能 |

---

## 四、如何使用技能

- 当你的请求**命中某技能的领域或触发场景**时，助手会自动加载并执行该技能的标准工作流。
- 例如：要新建一个 MES 列表页，助手会调用 `mes-list-page`；要生成提交记录，会调用 `git-commit`；处理一个 `.xlsx` 文件会调用 `xlsx`。
- 你也可以**显式要求**使用某个技能，例如"用 `git-commit` 帮我生成提交信息""用 `systematic-debugging` 排查这个报错"。

> 提示：项目技能随仓库演进，目录位于 `.codebuddy/skills/`；新增或调整项目技能可参考 `writing-skills` / `skill-creator`。
